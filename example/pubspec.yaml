name: example
description: Example app for testing responsive_dashboard
publish_to: "none" # Isso impede o Flutter de tentar publicar o exemplo no pub.dev

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  easy_flutter_charts: ^1.2.1
  easy_flutter_table: ^1.0.0
  responsive_dashboard:
    path: ../

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
